const initialState = {auth_url: ""};

const auth = (state = initialState, action) => {
    const { auth } = action;
    switch (action.type) {
        default:
            return state
    }
}
export default auth
